This is an automated maven project for building Rjms. A known issue with Maven will display errors in Eclipse, but that will not affect the build. Please use "clean install" to build the package.
Both the packages Rjms and Rjmsjars will be written to the /build directory.
