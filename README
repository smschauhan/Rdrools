This is an automated maven project for building Rdrools. A known issue with Maven will display errors in Eclipse, but that will not affect the build. Please use "clean install" to build the package.
Both the packages Rdrools and Rdroolsjars will be written to the /build directory.
